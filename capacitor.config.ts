import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.aicdismissal.vercel',
  appName: 'AIC-Dismissal',
  webDir: 'static',
  server: {
    androidScheme: 'https'
  }
};

export default config;
